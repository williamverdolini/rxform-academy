import { Routes } from '@angular/router';
import { AcademySummaryComponent } from './pages/academy-summary/academy-summary.component';

export const routes: Routes = [
  { path: '', component: AcademySummaryComponent },
];
